NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only llama.cpp base URL (no /v1 path). Set in .env.local.
# The server must be started separately with model alias qwen3-general.
AI_SERVICE_ENDPOINT=
